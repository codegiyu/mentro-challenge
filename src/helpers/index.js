import getStarArr from "./getStarArr"
import handleNextTransitions from "./handleNextTransitions"
import handlePrevTransitions from "./handlePrevTransitions"
import handleRotationChanges from "./handleRotationChanges"

const helpers = {
    getStarArr,
    handleNextTransitions,
    handlePrevTransitions,
    handleRotationChanges
}

export default helpers